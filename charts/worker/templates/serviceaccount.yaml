apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "worker.labels" . | nindent 4 }}
automountServiceAccountToken: false # consumers never talk to the Kubernetes API; Pod Identity needs no token mount
