# The mirror Jobs' identity: steakllm/mirror → role steakllm-mirror (infra/gpu): write the models
# bucket, push steakllm/vllm, and nothing else.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mirror
  namespace: steakllm
automountServiceAccountToken: false
